const LoginWithGoogle = () => {
    return (
        <button type={`submit`} name={`action`} value={`google`}>
            Sign in with Google
        </button>
    )
}

export default LoginWithGoogle