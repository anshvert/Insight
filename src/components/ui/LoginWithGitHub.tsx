const LoginWithGithub = () => {
    return (
        <button type={`submit`} name={`action`} value={`github`}>
            Sign in with Github
        </button>
    )
}

export default LoginWithGithub